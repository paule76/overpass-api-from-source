# Overpass API Version
# Use "latest" for the most recent version (recommended by Overpass developers)
# Or specify a version like "0.7.62.7"
# See available versions at: https://dev.overpass-api.de/releases/
OVERPASS_VERSION=latest

# Optional: Custom settings
# OVERPASS_META=yes
# OVERPASS_SPACE=4000000000
# OVERPASS_MAX_TIMEOUT=600